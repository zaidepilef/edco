---
import Base from "../../layouts/Base.astro";
import Button from "../../components/Button.astro";
import CtaBanner from "../../components/CtaBanner.astro";
import { getCollection, render } from "astro:content";
import type { GetStaticPaths } from "astro";

export const getStaticPaths = (async () => {
  const services = await getCollection("services");
  return services.map((service) => ({
    params: { slug: service.id },
    props: { service },
  }));
}) satisfies GetStaticPaths;

const { service } = Astro.props;
const { Content } = await render(service);

const isDiagnostico = service.id === "evalua-tu-empresa";
const ctaHref = isDiagnostico ? "/edco/evalua-tu-empresa" : "/edco/contacto";
const ctaLabel = isDiagnostico ? "Evalúa tu Empresa" : "Contactar a EDCO";
---

<Base
  title={`${service.data.title} | EDCO Gestión Preventiva`}
  description={service.data.benefit}
>
  <section class="bg-ink text-white">
    <div class="mx-auto max-w-4xl px-4 py-16">
      <p class="text-sm font-bold uppercase tracking-widest text-brand-400">
        <a href="/edco/servicios" class="hover:underline">Servicios</a>
      </p>
      <h1 class="mt-4 text-4xl font-bold leading-tight tracking-tight sm:text-5xl">
        {service.data.title}
      </h1>
      <p class="mt-4 text-lg text-paper-100">{service.data.subtitle}</p>
    </div>
  </section>

  <section class="mx-auto max-w-4xl px-4 py-16">
    <div class="rounded-2xl border border-brand-200 bg-brand-50 p-6">
      <p class="font-semibold text-brand-800">Beneficio para tu empresa</p>
      <p class="mt-1 text-lg text-muted">{service.data.benefit}</p>
    </div>

    <article class="prose-edco mt-10 text-lg leading-relaxed text-muted">
      <Content />
    </article>

    <div class="mt-12">
      <Button href={ctaHref} variant="primary" size="lg" label={ctaLabel} />
    </div>
  </section>

  <CtaBanner
    title={isDiagnostico ? "Empieza hoy con tu diagnóstico" : "¿Quieres saber más?"}
    description="Te contactamos de forma inmediata para resolver tus dudas."
  />
</Base>
